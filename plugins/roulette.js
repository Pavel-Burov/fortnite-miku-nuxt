// This plugin handles additional roulette functionality if needed\nexport default ({ app }, inject) => {\n  // You could add additional global methods here if needed\n  // For example, a method to track analytics, or handle special URL parameters\n  \n  // Check if running on client side\n  if (process.client) {\n    // Add event listener for escape key to close modals\n    document.addEventListener('keydown', (e) => {\n      if (e.key === 'Escape') {\n        const modals = document.querySelectorAll('.modal.show');\n        modals.forEach(modal => {\n          modal.classList.remove('show');\n          document.body.classList.remove('no-scroll');\n        });\n      }\n    });\n    \n    // Add event delegation for modal background clicks\n    document.addEventListener('click', (e) => {\n      if (e.target.classList.contains('modal')) {\n        e.target.classList.remove('show');\n        document.body.classList.remove('no-scroll');\n      }\n    });\n  }\n};